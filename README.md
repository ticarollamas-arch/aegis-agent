╔════════════════════════════╗
║       AEGIS PLATFORM       ║
║    DevSecOps CLI Toolkit   ║
╚════════════════════════════╝

# 🚀 AEGIS AUDIT

## 📌 Overview
Plataforma CLI corporativa para auditoria de segurança automatizada utilizando múltiplos agentes de Inteligência Artificial (CrewAI + Ollama).

## 🧠 What this project does
- Plataforma CLI corporativa para auditoria de segurança automatizada utilizando múltiplos agentes de Inteligência Artificial (CrewAI + Ollama).

## 🏗 Architecture
O subsistema é desenhado seguindo práticas rígidas de engenharia defensiva:
- **CLI Layer**: Camada de terminal unificada operando sob o padrão de design system Aegis CLI.
- **Backend Logic**: Lógica funcional síncrona com mitigadores integrados contra exaustão de cotas.
- **Security Layer**: Filtros e mecanismos de escape para blindagem zero-trust.

## 📁 Project Structure
```text
├── .env.example
├── Dockerfile
├── cli/banner.py
├── cli/commands.py
├── cli/menu.py
├── core/config.py
├── core/engine.py
├── core/logger.py
├── docker-compose.yml
├── main.py
├── plugins/__init__.py
├── plugins/plugin_manager.py
├── requirements.txt
└── README.md
```

## ⚙️ Installation
Instalação direta e compatível nativamente com ambientes Linux, Kali Linux e Termux:

```bash
git clone <repo_url>
cd aegis-audit
pip install -r requirements.txt
```

## ▶️ Usage
Para inicializar e interagir com o subsistema corporativo:

```bash
aegis-audit ----target
```

## 🔐 Security Model
- **Classification**: DEFENSIVO / AUDITORIA
- **Higiene Digital**: Análise Heurística e LLM-based Reasoning
- **Mitigações Ativas**:
  - *Ameaça:* Timeouts e Travamentos de Rede → *Mitigação:* Implementação estrita de timeouts em todas as requisições HTTP e tratamento hierárquico de exceções (Timeout, ConnectionError, HTTPError).

## 🧩 Dependencies
- **Plataforma/Runtime**: Python 3.11+
- **Dependências Externas**:
  - `typer` (^0.9.0): Construção de interfaces CLI robustas e tipadas
  - `rich` (^13.7.0): Renderização avançada de terminal, cores ANSI e tabelas
  - `crewai` (^0.28.0): Orquestração de agentes autônomos de IA
  - `requests` (^2.31.0): Comunicação HTTP com a API do Ollama e alvos

---
*Generated by AEGIS DevSecOps Generator System*
